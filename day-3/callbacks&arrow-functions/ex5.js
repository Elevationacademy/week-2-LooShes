const capitalize = name => console.log(name.slice(0, 1).toUpperCase() + name.slice(1, name.length).toLowerCase())

capitalize("bOb") // returns Bob
capitalize("TAYLOR") // returns Taylor
capitalize("feliSHIA") // returns Felishia
